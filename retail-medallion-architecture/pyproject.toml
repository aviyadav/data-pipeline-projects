[project]
name = "retail-medallion-architecture"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "duckdb>=1.5.5",
    "pytz>=2026.3.post1",
    "tabulate>=0.10.0",
]

[project.scripts]
retail-medallion-architecture = "retail_medallion_architecture:main"
generate-orders = "retail_medallion_architecture.generate_orders:main"
pipeline = "retail_medallion_architecture.pipeline:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
